Hey!

This is a submission for a JetBrains Internship!

This solution is a ready-to-open program in Pycharm.

It contains the main code -> skip-gram-model-v1.0.py

And also a verification and visualization program -> ResultsChecking.py 
(this is here only for checking purposes and should not be treated as a part 
of the main code - mainly because it is not pure NumPy which was suggested in a task description)

The processing and download of the files is implemented in the code itself.
(the "text8" file and the .zip of it has to be downloaded separately because
of the size of these files)

There are files with extensions .json and .npz which are saved sample versions of a trained model
and can be used to load the model to the checker instantly.

:]
